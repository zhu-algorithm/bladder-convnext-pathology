# Results status

No independent-test performance is currently reported because the supplied
files do not contain accessible images, a frozen checkpoint, or test prediction
probabilities. This directory intentionally contains only dataset audits and
fold-design artifacts until real inference is run.

